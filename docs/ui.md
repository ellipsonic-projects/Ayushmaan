## Instructions to change the UI/UX of the product

1. Change the UI layouts of the pages for each SUPER_ADMIN `apps/web/app/(platform)/(superadmin)/*`, TENANT_ADMIN `apps/web/app/(tenant)/[slug]/tenant/(admin)`, CONSULTANTS `apps/web/app/(tenant)/[slug]/tenant/(consultant)` and Clients `apps/web/app/(platform)/(client)`

2. Sidebar

- Change the styling of the Sidebar with a button to either collapse the sidebar or keep it extended
- Make sure you display the logo `apps/web/public/logo.jpeg`
- Keep a lighter color

3. Navbar

- Restyle the navbar.

4. Layout of the platform should be easy to use with minimal clicks for each feature.

Make sure the styling is out of the box. Make sure the you use an uncommon styling. Keep it unique and aesthetic.

## NOTE : Keep the designs flexible for Desktop and Mobile Screens. i
