## 1. Make sure the TENANT_ADMIN has the privileges to perform CRUD operations on every object in the TENANT_ADMIN, CONSULTANT, CLIENT workspace

## 2. Make sure the UI is responsive and appealing for both desktop and mobile screens
